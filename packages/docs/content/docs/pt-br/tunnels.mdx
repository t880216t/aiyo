---
title: Túneis
description: Exponha o AiYo com segurança para acesso remoto e móvel.
---

# Túneis

Um túnel é um link público para o seu AiYo, para que você possa acessá-lo pelo celular ou de outra rede. Use `aiyo tunnel` para criar um para uma instância em execução.

## Pré-requisitos

O AiYo inicia o CLI do provedor de túnel na sua máquina. Instale primeiro o provedor que você quer usar:

```bash
brew install cloudflared
brew install ngrok
```

Os túneis rápidos da Cloudflare funcionam com `cloudflared`. O Ngrok exige uma conta ngrok e um authtoken do painel do ngrok:

```bash
ngrok config add-authtoken <your-ngrok-token>
```

## Início rápido

1. Inicie o AiYo:

```bash
aiyo
```

Se você pular esta etapa, `aiyo tunnel start` pode iniciar automaticamente um servidor CLI. Ao iniciar automaticamente, você pode passar opções do servidor como `--port`, `--host`, `--lan`, `--ui-password` e `--api-only`.

2. Inicie um túnel da Cloudflare:

```bash
aiyo tunnel start --provider cloudflare --mode quick
```

Ou inicie um túnel do Ngrok:

```bash
aiyo tunnel start --provider ngrok --mode quick
```

3. Verifique o status:

```bash
aiyo tunnel status
```

Quando o túnel estiver no ar, o `status` mostra uma URL pública. Abra-a, ou escaneie o QR code, para acessar o AiYo de qualquer lugar.

Por padrão, o AiYo imprime um QR code em sessões TTY interativas. Use `--qr` para forçar a saída do QR, ou `--no-qr` para desativá-la.

## Provedores

- `cloudflare`: modos quick, managed remote e managed local
- `ngrok`: modo quick

## Modos gerenciados

### Gerenciado remoto

Use um token + nome de host gerenciados pela Cloudflare:

```bash
aiyo tunnel start --provider cloudflare --mode managed-remote --token-file ~/.secrets/cf-token --hostname app.example.com
```

### Gerenciado local

Use uma configuração local do `cloudflared`:

```bash
aiyo tunnel start --provider cloudflare --mode managed-local --config ~/.cloudflared/config.yml
```

## Perfis (managed-remote)

Salve um perfil reutilizável:

```bash
aiyo tunnel profile add --provider cloudflare --mode managed-remote --name prod-main --hostname app.example.com --token-file ~/.secrets/cf-token
```

Inicie usando o perfil salvo:

```bash
aiyo tunnel start --profile prod-main
```

## Comandos úteis

```bash
aiyo tunnel providers
aiyo tunnel ready --provider cloudflare
aiyo tunnel ready --provider ngrok
aiyo tunnel doctor --provider cloudflare
aiyo tunnel doctor --provider ngrok
aiyo tunnel stop --port 3000
```

## Notas de comportamento

- um único túnel ativo por instância do AiYo (porta)
- iniciar um novo modo/provedor na mesma instância substitui o túnel anterior
- gerar um novo link de conexão revoga o anterior não utilizado
- o auto-start do túnel preserva flags do servidor como `--ui-password` e `--api-only` nas configurações da instância usadas por reinícios e atualizações

## Relacionado

- [Segurança](/pt-br/security/) — proteja a interface antes de expô-la
- [Túneis no desktop](/pt-br/desktop-tunnels/) — configuração de túnel no app desktop sem iniciar pelo CLI
- [PWA e Acesso Móvel](/pt-br/mobile/) — acesse o AiYo pelo celular
- [Solução de problemas](/pt-br/troubleshooting/) — se o link do túnel não funcionar
