---
title: Підключення до OpenCode
description: Виправте проблему, коли AiYo не підключається до свого сервера OpenCode.
---

# Підключення до OpenCode

Якщо AiYo завантажується, але ніяк не виходить за межі стану «OpenCode перезапускається», або чат не відповідає, сервер, із яким він спілкується, недоступний. Пройдіться по цьому.

## Він застряг на «OpenCode перезапускається»

- дайте йому хвилину одразу після запуску — цей стан нормальний, поки сервер стартує
- перевірте, чи сервер живий, командою `aiyo status`
- перезапустіть його командою `aiyo restart`
- перегляньте деталі запуску командою `aiyo logs`

## Ви підключаєтеся до власного сервера

Якщо ви налаштували AiYo на використання наявного сервера, ще раз перевірте налаштування з [OpenCode Server](/uk/opencode-server/):

- `OPENCODE_HOST` має містити порт, без шляху — наприклад, `http://localhost:4096`
- задайте `OPENCODE_SKIP_START=true`, щоб AiYo не запускав ще й свій
- якщо адреса недійсна, AiYo ігнорує її й натомість запускає власний сервер — шукайте попередження `[config]` у журналах

## Усе ще не працює

- переконайтеся, що версія Node.js — `20` або новіша
- перевстановіть найновіший CLI
- відкрийте `http://localhost:3000` напряму, перш ніж тестувати будь-який тунель чи віддалене посилання

## Пов'язане

- [OpenCode Server](/uk/opencode-server/) — як AiYo знаходить сервер і керує ним
- [Усунення несправностей](/uk/troubleshooting/) — інші поширені проблеми
